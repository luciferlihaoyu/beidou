# Test-only dependencies for the Beidou backend.
# Install into the venv with:  uv pip install -r requirements-dev.txt
-r requirements.txt

pytest==8.3.5
pytest-asyncio==0.25.3
httpx==0.27.0  # already pinned in requirements.txt; repeated here for completeness
